# Wax seal stamp (SVG)

Exported **vector wax disc** with **signature image** clipped inside (tinted via `feColorMatrix`).
Pair with `../mj-signature-silver/mj-signature-source.png` (same asset v0 used inside the seal).

The HTML used `waxstamp` wrapper + inline SVG. Replace `href="./brand/signature.png"` (or relative path) when inlining in a new site.
