# Reference — preserved from v0 export

This directory is the **only** content carried forward on `main` for the rebuild.

1. **essay-footnote-pattern/** — Essay margin footnote UX: DOM contract, coordinator script, bundled essay/footnote CSS from the legacy export.
2. **wax-seal/** — Wax disc + signature treatment (SVG).
3. **mj-signature-silver/** — Signature master PNG + header webp used on the old site.
4. **tarot-archetype-illustrations/** — Three tarot artworks (Logic, Psyche, Instinct).

The full static site lives on the **`legacy`** branch (frozen).
